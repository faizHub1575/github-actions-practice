on:
    push:
        branches:
            - main

jobs:
    greet:
        runs-on: ubuntu-latest
        steps:
            - name: checkout code
              uses: actions/checkout@v2
            
            - name: say hello
              run: echo "Hello, World!"
             

